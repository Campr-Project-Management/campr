{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">
                    <h2>{{ 'admin.project_cost_type.list.title'|trans }}<small>{{ project_cost_type.name }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_project_cost_type_create') }}">{{ 'admin.project_cost_type.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_cost_type_edit', {'id': project_cost_type.id}) }}">{{ 'admin.general.form.button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_cost_type_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>{{ 'admin.project_cost_type.list.table.name'|trans }}</th>
                                    <td>{{ project_cost_type.name }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_cost_type.list.table.project'|trans }}</th>
                                    <td>
                                        {% if project_cost_type.project %}
                                            {{ project_cost_type.project.name }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_cost_type.list.table.sequence'|trans }}</th>
                                    <td>{{ project_cost_type.sequence }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_cost_type.list.table.created_at'|trans }}</th>
                                    <td>
                                        {% if project_cost_type.createdAt %}
                                            {{ project_cost_type.createdAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_cost_type.list.table.updated_at'|trans }}</th>
                                    <td>
                                        {% if project_cost_type.updatedAt %}
                                            {{ project_cost_type.updatedAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
