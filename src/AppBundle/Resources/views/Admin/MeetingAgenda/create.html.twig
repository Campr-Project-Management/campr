{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% form_theme form 'AppBundle:Admin/Layout/Partials:form_fields.html.twig' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.meeting_agenda.create.title'|trans }}</h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_meeting_agenda_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body card-padding">
                    {{ form_start(
                        form,
                        {
                            'method': 'POST',
                            'action': path('app_admin_meeting_agenda_create'),
                            'attr': {
                                'id': 'create-form',
                                'class': 'form-horizontal',
                                'novalidate': 'novalidate',
                            },
                        }
                    ) }}
                    {% if form.vars.valid == false %}
                        <div class="alert alert-danger">
                            {% for field in form.children %}
                                {{ form_errors(field) }}
                            {% endfor %}
                        </div>
                    {% endif %}
                    {{ form_widget(
                        form.meeting,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.meeting_agenda.create.meeting'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.topic,
                        {
                            'attr': {
                                'class': 'form-control input-sm',
                            },
                            'label': 'admin.meeting_agenda.create.topic'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.responsibility,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.meeting_agenda.create.responsible'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.start,
                        {
                            'attr': {
                                'class': 'form-control time-picker',
                                'data-date-format': "HH:mm",
                                'addon_front': 'time',
                            },
                            'label': 'admin.meeting_agenda.create.start'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.end,
                        {
                            'attr': {
                                'class': 'form-control time-picker',
                                'data-date-format': "HH:mm",
                                'addon_front': 'time',
                            },
                            'label': 'admin.meeting_agenda.create.end'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.duration,
                        {
                            'attr': {
                                'class': 'form-control time-picker',
                                'data-date-format': "HH:mm",
                                'addon_front': 'time',
                            },
                            'label': 'admin.meeting_agenda.create.duration'|trans
                        }
                    ) }}

                    {% include '@App/Admin/Layout/Partials/form_create_button.html.twig' %}

                    <span class="hidden">{{ form_row(form._token) }}</span>
                    {{ form_end(form, {'render_rest': false}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
