{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'messages' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'title.project_module.list'|trans }}<small>{{ project_module.module }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_project_module_create') }}">{{ 'add.project_module'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_module_edit', {'id': project_module.id}) }}">{{ 'button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_module_list') }}">{{ 'button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>{{ 'table_header_cell.module'|trans }}</th>
                                    <td>{{ project_module.module }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'table_header_cell.project'|trans }}</th>
                                    <td>
                                        {% if project_module.project %}
                                            {{ project_module.project.name }}
                                        {% else %}
                                            -
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'table_header_cell.enabled'|trans }}</th>
                                    <td>
                                        {% if project_module.isEnabled %}
                                            {{ 'message.yes'|trans }}
                                        {% else %}
                                            {{ 'message.no'|trans }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'table_header_cell.required'|trans }}</th>
                                    <td>
                                        {% if project_module.isRequired %}
                                            {{ 'message.yes'|trans }}
                                        {% else %}
                                            {{ 'message.no'|trans }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'table_header_cell.printable'|trans }}</th>
                                    <td>
                                        {% if project_module.isPrint %}
                                            {{ 'message.yes'|trans }}
                                        {% else %}
                                            {{ 'message.no'|trans }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'table_header_cell.created_at'|trans }}</th>
                                    <td>
                                        {% if project_module.createdAt %}
                                            {{ project_module.createdAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'table_header_cell.updated_at'|trans }}</th>
                                    <td>
                                        {% if project_module.updatedAt %}
                                            {{ project_module.updatedAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
