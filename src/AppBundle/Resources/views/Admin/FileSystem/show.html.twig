{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.filesystem.list.title'|trans }}<small>{{ fileSystem.name|default('-') }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="#" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_filesystem_create') }}">{{ 'admin.filesystem.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_filesystem_edit', {'id': fileSystem.id}) }}">{{ 'admin.general.form.button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_filesystem_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>{{ 'admin.filesystem.list.table.id'|trans }}</th>
                                    <td>{{ fileSystem.id }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.filesystem.list.table.name'|trans }}</th>
                                    <td>{{ fileSystem.name|default('-') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.filesystem.list.table.project'|trans }}</th>
                                    <td>{{ fileSystem.project.name|default('-') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.filesystem.list.table.driver'|trans }}</th>
                                    <td>{{ fileSystem.driver }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.filesystem.list.table.config'|trans }}</th>
                                    <td>
                                        {% for config in fileSystem.config %}
                                            <p>{{ config }}</p>
                                        {% endfor %}
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
