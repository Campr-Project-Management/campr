{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'messages' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'title.communication.list'|trans }}<small>{{ communication.meetingName }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_communication_create') }}">{{ 'add.communication'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_communication_edit', {'id': communication.id}) }}">{{ 'button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_communication_list') }}">{{ 'button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                            <tr>
                                <th>{{ 'table_header_cell.id'|trans }}</th>
                                <td>{{ communication.id }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.project'|trans }}</th>
                                <td>{{ communication.project.name|default('-') }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.meeting_name'|trans }}</th>
                                <td>{{ communication.meetingName }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.content'|trans }}</th>
                                <td>{{ communication.content }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.schedule'|trans }}</th>
                                <td>{{ communication.schedule.name|default('-') }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.location'|trans }}</th>
                                <td>{{ communication.location }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.participants'|trans }}</th>
                                <td>
                                   {% for user in communication.participants %}
                                        <p>@{{ user.username }} - {{ user.fullName }}</p>
                                   {% endfor %}
                                </td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.created_at'|trans }}</th>
                                <td>{{ communication.createdAt|date('d/m/Y') }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'table_header_cell.updated_at'|trans }}</th>
                                <td>{{ communication.updatedAt|date('d/m/Y') }}</td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
