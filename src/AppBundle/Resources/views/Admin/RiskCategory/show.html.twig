{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.risk_category.list.title'|trans }}<small>{{ category.name }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_risk_category_create') }}">{{ 'admin.risk_category.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_risk_category_edit', {'id': category.id}) }}">{{ 'admin.general.form.button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_risk_category_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body m-t-10 card-padding">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                            <tr>
                                <th>{{ 'admin.risk_category.list.table.id'|trans }}</th>
                                <td>{{ category.id }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'admin.risk_category.list.table.name'|trans }}</th>
                                <td>{{ category.name }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'admin.risk_category.list.table.sequence'|trans }}</th>
                                <td>{{ category.sequence }}</td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
