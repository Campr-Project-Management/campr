{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.note.list.title'|trans }}<small>{{ note.title }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_note_create') }}">{{ 'admin.note.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_note_edit', {'id': note.id}) }}">{{ 'admin.general.form.button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_note_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>{{ 'admin.note.list.table.id'|trans }}</th>
                                    <td>{{ note.id }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.title'|trans }}</th>
                                    <td>{{ note.title }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.project'|trans }}</th>
                                    <td>{{ note.project.name|default('-') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.meeting'|trans }}</th>
                                    <td>{{ note.meeting.name|default('-') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.description'|trans }}</th>
                                    <td>{{ note.description|default('-') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.show_in_report'|trans }}</th>
                                    <td>
                                        {% if note.showInStatusReport %}
                                            <span class="c-green">{{ 'admin.general.yes'|trans }}</span>
                                        {% else %}
                                            <span class="c-red">{{ 'admin.general.no'|trans }}</span>
                                        {% endif %}
                                    </td>                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.responsible'|trans }}</th>
                                    <td>{{ note.responsibility.username|default('-') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.date'|trans }}</th>
                                    <td>{{ note.date|date('d/m/Y') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.due_date'|trans }}</th>
                                    <td>{{ note.dueDate|date('d/m/Y') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.created_at'|trans }}</th>
                                    <td>{{ note.createdAt|date('d/m/Y') }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.note.list.table.updated_at'|trans }}</th>
                                    <td>{{ note.updatedAt|date('d/m/Y') }}</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
