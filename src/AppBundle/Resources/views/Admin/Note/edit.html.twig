{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% form_theme form 'AppBundle:Admin/Layout/Partials:form_fields.html.twig' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.note.edit.title'|trans }}</h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_note_create') }}">{{ 'admin.note.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_note_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body card-padding">
                    {{ form_start(
                        form,
                        {
                            'method': 'POST',
                            'action': path('app_admin_note_edit', {'id': id}),
                            'attr': {
                                'class': 'form-horizontal',
                                'novalidate': 'novalidate',
                            },
                        }
                    ) }}
                    {% if form.vars.valid == false %}
                        <div class="alert alert-danger">
                            {% for field in form.children %}

                                {{ form_errors(field) }}
                            {% endfor %}
                        </div>
                    {% endif %}
                    {{ form_widget(
                    form.title,
                        {
                            'attr': {
                                'class': 'form-control input-sm',
                            },
                            'label': 'admin.note.create.note_title'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.description,
                        {
                            'attr': {
                                'class': 'form-control',
                            },
                            'label': 'admin.note.create.description'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.project,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.note.create.project'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.meeting,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.note.create.meeting'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.responsibility,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.note.create.responsible'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.status,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.note.create.status'|trans,
                        }
                    ) }}
                    {{ form_widget(
                        form.date,
                        {
                            'attr': {
                                'class': 'form-control date-picker',
                                'addon_front': 'calendar',
                            },
                        'label': 'admin.note.create.date'|trans
                    }
                    ) }}
                    {{ form_widget(
                        form.dueDate,
                        {
                            'attr': {
                                'class': 'form-control date-picker',
                                'addon_front': 'calendar',
                            },
                            'label': 'admin.note.create.due_date'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.showInStatusReport,
                        {
                            'attr': {
                                'class': 'form-control input-sm',
                                'toggle': true
                            },
                            'label': 'admin.note.edit.show_in_status'|trans
                        }
                    ) }}

                    {% include '@App/Admin/Layout/Partials/form_edit_buttons.html.twig'
                        with {
                            'delete_path': 'app_admin_note_delete',
                            'id': id,
                        }
                    %}

                    <span class="hidden">{{ form_row(form._token) }}</span>
                    {{ form_end(form, {'render_rest': false}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
