{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="card">
        <div class="card-header bgm-teal">
            <h2>{{ 'admin.raci.list.title'|trans }}<small>{{ 'admin.raci.list.subtitle'|trans }}</small></h2>
            <a href="{{ path('app_admin_todo_create') }}">
                <button class="btn bgm-red btn-float waves-effect"><i class="zmdi zmdi-plus"></i></button>
            </a>
        </div>
        <div class="card-body">
            {% for flash_message in app.session.flashBag.get('success') %}
                <div id="general-alert" class="alert alert-success m-t-15">
                    <strong>{{ flash_message }}</strong>
                </div>
            {% endfor %}
            <div id="delete-item-alert" class="alert alert-success m-t-15 hidden">
                <strong>
                    {{ 'admin.raci.delete.success.first'|trans }}
                    <span id="object-id">
                        {% for flash_message in app.session.flashBag.get('success') %}
                            {{ flash_message }}
                        {% endfor %}
                    </span>
                    {{ 'admin.raci.delete.success.second'|trans }}
                </strong>
            </div>
            <div class="table-responsive">
                <table
                        id="data-table-command"
                        class="table table-condensed table-striped table-vmiddle"
                        data-url="{{ path('app_admin_todo_list_filtered') }}"
                        data-show="app_admin_todo_show"
                        data-edit="app_admin_todo_edit"
                        data-delete="app_admin_todo_delete">
                    <thead>
                    <tr>
                        <th data-column-id="id" data-identifier="true" data-type="numeric">{{ 'admin.todo.list.table.id'|trans }}</th>
                        <th data-column-id="title">{{ 'admin.todo.list.table.title'|trans }}</th>
                        <th data-column-id="projectName">{{ 'admin.todo.list.table.project'|trans }}</th>
                        <th data-column-id="responsibilityFullName">{{ 'admin.todo.list.table.responsible'|trans }}</th>
                        <th data-column-id="date">{{ 'admin.todo.list.table.date'|trans }}</th>
                        <th data-column-id="dueDate">{{ 'admin.todo.list.table.due_date'|trans }}</th>
                        <th data-column-id="status">{{ 'admin.todo.list.table.status'|trans }}</th>
                        <th data-column-id="commands" data-formatter="commands" data-sortable="false">{{ 'admin.general.placeholder.actions'|trans }}</th>
                    </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <!-- Data Table -->
    <script src="{{ asset('bundles/app/assets/js/data_table.js') }}"></script>
    <script src="{{ asset('bundles/app/assets/js/alert_messages.js') }}"></script>
{% endblock %}
