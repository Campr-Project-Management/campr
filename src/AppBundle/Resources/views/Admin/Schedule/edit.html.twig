{% extends 'AppBundle:Admin:base.html.twig' %}
{% trans_default_domain 'admin' %}
{% block body_content %}
    {{ form_start(
        form,
        {
            'method': 'POST',
            'action': path('app_admin_schedule_edit', {'id': id}),
            'attr': {
                'novalidate': 'novalidate',
            },
        }
    ) }}
    {% if form.vars.valid == false %}
        <div class="alert alert-danger">
            {% for field in form.children %}
                {{ form_errors(field) }}
            {% endfor %}
        </div>
    {% endif %}
    <div class="form-group">
        <label for="name">{{ 'admin.schedule.create.name'|trans }}:</label>
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        <button class="btn btn-default" type="submit">{{ 'admin.schedule.list.edit'|trans }}</button>
        <a class="btn btn-default" href="{{ path('app_admin_schedule_delete', {'id': id}) }}">
            {{ 'admin.schedule.list.delete'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
