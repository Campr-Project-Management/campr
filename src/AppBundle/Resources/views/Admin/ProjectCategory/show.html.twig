{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.project_category.list.title'|trans }}<small>{{ project_category.name }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_project_category_create') }}">{{ 'admin.project_category.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_category_edit', {'id': project_category.id}) }}">{{ 'admin.general.form.button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_category_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>{{ 'admin.project_category.list.table.name'|trans }}</th>
                                    <td>{{ project_category.name }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_category.list.table.project'|trans }}</th>
                                    <td>
                                        {% if project_category.project %}
                                            {{ project_category.project.name }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_category.list.table.sequence'|trans }}</th>
                                    <td>{{ project_category.sequence }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_category.list.table.created_at'|trans }}</th>
                                    <td>
                                        {% if project_category.createdAt %}
                                            {{ project_category.createdAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_category.list.table.updated_at'|trans }}</th>
                                    <td>
                                        {% if project_category.updatedAt %}
                                            {{ project_category.updatedAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
