{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">
                    <h2>{{ 'admin.project_category.list.title'|trans }}<small>{{ 'admin.project_category.list.subtitle'|trans }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_project_category_create') }}">{{ 'admin.project_category.list.add'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                    {% for flash_message in app.session.flashBag.get('success') %}
                        <div class="alert alert-success m-t-15">
                            <strong>{{ flash_message }}</strong>
                        </div>
                    {% endfor %}
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>{{ 'admin.project_category.list.table.id'|trans }}</th>
                                    <th>{{ 'admin.project_category.list.table.name'|trans }}</th>
                                    <th>{{ 'admin.project_category.list.table.project'|trans }}</th>
                                    <th>{{ 'admin.project_category.list.table.sequence'|trans }}</th>
                                    <th>{{ 'admin.project_category.list.table.created_at'|trans }}</th>
                                    <th>{{ 'admin.project_category.list.table.updated_at'|trans }}</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for project_category in project_categories %}
                                <tr>
                                    <td class="f-500 c-cyan">{{ project_category.id }}</td>
                                    <td>{{ project_category.name }}</td>
                                    <td>
                                        {% if project_category.project %}
                                            {{ project_category.project.name }}
                                        {% endif %}
                                    </td>
                                    <td>{{ project_category.sequence }}</td>
                                    <td>
                                        {% if project_category.createdAt %}
                                            {{ project_category.createdAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if project_category.updatedAt %}
                                            {{ project_category.updatedAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% include '@App/Admin/Layout/Partials/form_list_buttons.html.twig'
                                            with {
                                                'show_path': 'app_admin_project_category_show',
                                                'edit_path': 'app_admin_project_category_edit',
                                                'delete_path': 'app_admin_project_category_delete',
                                                'id': project_category.id,
                                            }
                                        %}
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="7" class="f-500 c-cyan">{{ 'admin.project_category.list.not_found'|trans }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
