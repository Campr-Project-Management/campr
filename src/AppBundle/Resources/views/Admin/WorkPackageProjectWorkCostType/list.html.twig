{% extends 'AppBundle:Admin:base.html.twig' %}
{% trans_default_domain 'admin' %}
{% block body_content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.wppcwct.list.title'|trans }}<small>{{ 'admin.wppcwct.list.subtitle'|trans }}</small></h2>
                    <a href="{{ path('app_admin_wppcwct_create') }}">
                        <button class="btn bgm-red btn-float waves-effect"><i class="zmdi zmdi-plus"></i></button>
                    </a>
                </div>
                <div class="card-body m-t-0">
                    {% for flash_message in app.session.flashBag.get('success') %}
                        <div class="alert alert-success">
                            <strong>{{ flash_message }}</strong>
                        </div>
                    {% endfor %}
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>{{ 'admin.wppcwct.list.table.id'|trans }}</th>
                                    <th>{{ 'admin.wppcwct.list.table.name'|trans }}</th>
                                    <th>{{ 'admin.wppcwct.list.table.workpackage'|trans }}</th>
                                    <th>{{ 'admin.wppcwct.list.table.projectworkcosttype'|trans }}</th>
                                    <th>{{ 'admin.wppcwct.list.table.created_at'|trans }}</th>
                                    <th>{{ 'admin.general.placeholder.actions'|trans }}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for workprojecttype in workProjectTypes %}
                                    <tr>
                                        <td class="f-500 c-cyan">{{ workprojecttype.id }}</td>
                                        <td>{{ workprojecttype.name }}</td>
                                        <td>{{ workprojecttype.createdAt|date('d/m/Y') }}</td>
                                        <td>{{ workprojecttype.workPackage.name|default('-') }}</td>
                                        <td>{{ workprojecttype.projectWorkCostType.name|default('-') }}</td>

                                        <td>
                                            {% include '@App/Admin/Layout/Partials/form_list_buttons.html.twig'
                                                with {
                                                    'show_path': 'app_admin_wppcwct_show',
                                                    'edit_path': 'app_admin_wppcwct_edit',
                                                    'delete_path': 'app_admin_wppcwct_delete',
                                                'id': workprojecttype.id,
                                                }
                                            %}
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="6" class="f-500 c-cyan">{{ 'admin.wppcwct.list.no-wppcwct'|trans }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                <div id="recent-items-chart" class="flot-chart"></div>
            </div>
        </div>
    </div>
{% endblock %}
