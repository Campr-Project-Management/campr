{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="card">
        <div class="card-header bgm-teal">
            <h2>{{ 'admin.decision.list.title'|trans }}<small>{{ 'admin.decision.list.subtitle'|trans }}</small></h2>
            <a href="{{ path('app_admin_decision_create') }}">
                <button class="btn bgm-red btn-float waves-effect"><i class="zmdi zmdi-plus"></i></button>
            </a>
        </div>
        <div class="card-body">
            {% for flash_message in app.session.flashBag.get('success') %}
                <div id="general-alert" class="alert alert-success m-t-15">
                    <strong>{{ flash_message }}</strong>
                </div>
            {% endfor %}
            <div id="delete-item-alert" class="alert alert-success m-t-15 hidden">
                <strong>
                    {{ 'admin.decision.delete.success.first'|trans }}
                    <span id="object-id">
                        {% for flash_message in app.session.flashBag.get('success') %}
                            {{ flash_message }}
                        {% endfor %}
                    </span>
                    {{ 'admin.decision.delete.success.second'|trans }}
                </strong>
            </div>
            <div class="table-responsive">
                <table id="data-table-command" class="table table-condensed table-striped table-vmiddle" data-url="{{ path('app_admin_decision_list_filtered') }}" data-show="app_admin_decision_show" data-edit="app_admin_decision_edit" data-delete="app_admin_decision_delete">
                    <thead>
                        <tr>
                            <th data-column-id="id" data-identifier="true" data-type="numeric">{{ 'admin.decision.list.table.id'|trans }}</th>
                            <th data-column-id="title">{{ 'admin.decision.list.table.title'|trans }}</th>
                            <th data-column-id="project">{{ 'admin.decision.list.table.project'|trans }}</th>
                            <th data-column-id="responsibility">{{ 'admin.decision.list.table.responsibility'|trans }}</th>
                            <th data-column-id="date">{{ 'admin.decision.list.table.date'|trans }}</th>
                            <th data-column-id="dueDate">{{ 'admin.decision.list.table.due_date'|trans }}</th>
                            <th data-column-id="commands" data-formatter="commands" data-sortable="false">{{ 'admin.general.placeholder.actions'|trans }}</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <!-- Data Table -->
    <script src="{{ asset('bundles/app/assets/js/data_table.js') }}"></script>
    <script src="{{ asset('bundles/app/assets/js/alert_messages.js') }}"></script>
{% endblock %}
