{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.project_complexity.list.title'|trans }}<small>{{ project_complexity.name }}</small></h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_project_complexity_create') }}">{{ 'admin.project_complexity.list.add'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_complexity_edit', {'id': project_complexity.id}) }}">{{ 'admin.general.form.button.edit'|trans }}</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_admin_project_complexity_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>{{ 'admin.project_complexity.list.table.name'|trans }}</th>
                                    <td>{{ project_complexity.name }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_complexity.list.table.project'|trans }}</th>
                                    <td>
                                        {% if project_complexity.project %}
                                            {{ project_complexity.project.name }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_complexity.list.table.sequence'|trans }}</th>
                                    <td>{{ project_complexity.sequence }}</td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_complexity.list.table.created_at'|trans }}</th>
                                    <td>
                                        {% if project_complexity.createdAt %}
                                            {{ project_complexity.createdAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr>
                                    <th>{{ 'admin.project_complexity.list.table.updated_at'|trans }}</th>
                                    <td>
                                        {% if project_complexity.updatedAt %}
                                            {{ project_complexity.updatedAt|date('d/m/Y') }}
                                        {% endif %}
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
