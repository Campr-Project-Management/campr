{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% form_theme form 'AppBundle:Admin/Layout/Partials:form_fields.html.twig' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-8 col-md-offset-2">
            <div class="card">
                <div class="card-header bgm-teal">
                    <h2>{{ 'admin.assignment.create.title'|trans }}</h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert c-white"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_assignment_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body m-t-0 card-padding">
                    {{ form_start(
                        form,
                        {
                            'method': 'POST',
                            'action': path('app_admin_assignment_create'),
                            'attr': {
                                'id': 'create-form',
                                'class': 'form-horizontal',
                                'novalidate': 'novalidate',
                            },
                        }
                    ) }}
                    {% if form.vars.valid == false %}
                        <div class="alert alert-danger">
                            {% for field in form.children %}
                                {{ form_errors(field) }}
                            {% endfor %}
                        </div>
                    {% endif %}
                    {{ form_widget(
                        form.workPackage,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.assignment.create.workpackage'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.workPackageProjectWorkCostType,
                        {
                            'attr': {
                                'class': 'selectpicker',
                            },
                            'label': 'admin.assignment.create.wppwct'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.milestone,
                        {
                            'attr': {
                                'class': 'form-control input-sm',
                            },
                            'label': 'admin.assignment.create.milestone'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.percentWorkComplete,
                        {
                            'attr': {
                                'class': 'form-control input-sm',
                            },
                            'label': 'admin.assignment.create.percent_work'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.startedAt,
                        {
                            'attr': {
                                'class': 'form-control date-picker',
                                'addon_front': 'calendar',
                            },
                            'label': 'admin.assignment.create.started_at'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.finishedAt,
                        {
                            'attr': {
                                'class': 'form-control date-picker',
                                'addon_front': 'calendar',
                            },
                            'label': 'admin.assignment.create.finished_at'|trans
                        }
                    ) }}
                    {{ form_widget(
                        form.confirmed,
                        {
                            'attr': {
                                'class': 'form-control input-sm',
                                'toggle': true
                            },
                            'label': 'admin.assignment.create.confirmed'|trans
                        }
                    ) }}

                    {% include '@App/Admin/Layout/Partials/form_create_button.html.twig' %}

                    <span class="hidden">{{ form_row(form._token) }}</span>
                    {{ form_end(form, {'render_rest': false}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
