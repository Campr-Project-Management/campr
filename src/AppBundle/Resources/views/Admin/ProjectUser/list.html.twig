{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% block body_content %}
    <div class="card">
        <div class="card-header bgm-teal">
            <h2>{{ 'admin.project_user.list.title'|trans }}<small>{{ 'admin.project_user.list.subtitle'|trans }}</small></h2>
            <a href="{{ path('app_admin_project_user_create') }}">
                <button class="btn bgm-red btn-float waves-effect"><i class="zmdi zmdi-plus"></i></button>
            </a>
        </div>
        <div class="card-body">
            {% for flash_message in app.session.flashBag.get('success') %}
                <div class="alert alert-success m-t-15">
                    <strong>{{ flash_message }}</strong>
                </div>
            {% endfor %}
            <div id="delete-item-alert" class="alert alert-success m-t-15 hidden">
                <strong>
                    {{ 'admin.project_user.delete.success.first'|trans }}
                    <span id="object-id">
                        {% for flash_message in app.session.flashBag.get('success') %}
                            {{ flash_message }}
                        {% endfor %}
                    </span>
                    {{ 'admin.project_user.delete.success.second'|trans }}
                </strong>
            </div>
            <div class="table-responsive">
                <table id="data-table-command" class="table table-condensed table-striped table-vmiddle" data-url="{{ path('app_admin_project_user_list_filtered') }}" data-show="app_admin_project_user_show" data-edit="app_admin_project_user_edit" data-delete="app_admin_project_user_delete">
                    <thead>
                        <tr>
                            <th data-column-id="id" data-identifier="true" data-type="numeric">{{ 'admin.project_user.list.table.id'|trans }}</th>
                            <th data-column-id="userFullName">{{ 'admin.project_user.list.table.user'|trans }}</th>
                            <th data-column-id="projectName">{{ 'admin.project_user.list.table.project'|trans }}</th>
                            <th data-column-id="projectCategoryName">{{ 'admin.project_user.list.table.project_category'|trans }}</th>
                            <th data-column-id="projectRoleName">{{ 'admin.project_user.list.table.project_role'|trans }}</th>
                            <th data-column-id="projectDepartmentName">{{ 'admin.project_user.list.table.project_department'|trans }}</th>
                            <th data-column-id="projectTeamName">{{ 'admin.project_user.list.table.project_team'|trans }}</th>
                            <th data-column-id="commands" data-formatter="commands" data-sortable="false">{{ 'admin.general.placeholder.actions'|trans }}</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <!-- Data Table -->
    <script src="{{ asset('bundles/app/assets/js/data_table.js') }}"></script>
    <script src="{{ asset('bundles/app/assets/js/alert_messages.js') }}"></script>
{% endblock %}
