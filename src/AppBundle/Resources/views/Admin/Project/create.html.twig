{% extends 'AppBundle:Admin:base.html.twig' %}

{% trans_default_domain 'admin' %}

{% form_theme form 'AppBundle:Admin/Layout/Partials:form_fields.html.twig' %}

{% block body_content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">
                    <h2>{{ 'admin.project.create.title'|trans }}</h2>
                    <ul class="actions">
                        <li class="dropdown">
                            <a href="" data-toggle="dropdown">
                                <i class="zmdi zmdi-more-vert"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right">
                                <li>
                                    <a href="{{ path('app_admin_project_list') }}">{{ 'admin.general.form.button.back_to_list'|trans }}</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="card-body card-padding">
                    {{ form_start(
                        form,
                        {
                            'method': 'POST',
                            'action': path('app_admin_project_create'),
                            'attr': {
                                'class': 'form-horizontal',
                                'novalidate': 'novalidate',
                            },
                        }
                    ) }}
                        {% if form.vars.valid == false %}
                            <div class="alert alert-danger">
                                {% for field in form.children %}
                                    {{ form_errors(field) }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        {{ form_widget(
                            form.name,
                            {
                                'attr': {
                                    'class': 'form-control input-sm',
                                    'placeholder': 'admin.general.placeholder.name'|trans,
                                },
                                'label': 'admin.project.create.name'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.number,
                            {
                                'attr': {
                                    'class': 'form-control input-sm',
                                    'placeholder': 'admin.general.placeholder.number'|trans,
                                },
                                'label': 'admin.project.create.number'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.sponsor,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.sponsor'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.manager,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.manager'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.company,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.company'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.projectComplexity,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.project_complexity'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.projectCategory,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.project_category'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.projectScope,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.project_scope'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.status,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.status'|trans,
                            }
                        ) }}
                        {{ form_widget(
                            form.portfolio,
                            {
                                'attr': {'class': 'form-control input-sm'},
                                'label': 'admin.project.create.portfolio'|trans,
                            }
                        ) }}

                        {% include '@App/Admin/Layout/Partials/form_create_button.html.twig' %}

                        <span class="hidden">{{ form_row(form._token) }}</span>
                    {{ form_end(form, {'render_rest': false}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
