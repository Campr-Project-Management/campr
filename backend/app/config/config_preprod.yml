imports:
    - { resource: config.yml }
    - { resource: bundles/nelmio_cors.yml }

parameters:
    database_name: campr_preprod
    database_password: campr
    domain: preprod.campr.biz
    uploads.root_folder: '%kernel.root_dir%/../../../../shared/web/uploads'

#framework:
#    validation:
#        cache: validator.mapping.cache.doctrine.apc
#    serializer:
#        cache: serializer.mapping.cache.doctrine.apc

doctrine:
    orm:
        metadata_cache_driver: apc
        result_cache_driver: apc
        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console
