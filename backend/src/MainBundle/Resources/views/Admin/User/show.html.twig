{% extends 'MainBundle:Admin:base.html.twig' %}

{% trans_default_domain 'messages' %}

{% block body_content %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                <p class="text-primary">
                    {{ user.fullName }}
                    <a href="{{ path('main_admin_user_list') }}" class="btn btn-sm btn-primary pull-right">{{ 'button.back_to_list'|trans }}</span></a>
                </p>
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="caption">
                <ul class="list-unstyled">
                    <li class="list-group-item">
                        <strong>{{ 'table_header_cell.id'|trans }}: </strong>{{ user.id }}
                    </li>
                    <li class="list-group-item">
                        <strong>{{ 'table_header_cell.full_name'|trans }}: </strong>{{ user.fullName }}
                    </li>
                    <li class="list-group-item">
                        <strong>{{ 'table_header_cell.username'|trans }}: </strong>{{ user.username }}
                    </li>
                    <li class="list-group-item">
                        <strong>{{ 'table_header_cell.email'|trans }}: </strong>{{ user.email }}
                    </li>
                    <li class="list-group-item">
                        <strong>{{ 'table_header_cell.phone'|trans }}: </strong>{{ user.phone }}
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
