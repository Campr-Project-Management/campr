{% trans_default_domain 'messages' %}

{{ form_widget(
    form.accessToken,
    {
        'attr': {
            'class': 'form-control input-sm',
        },
        'label': 'label.dropbox.key'|trans,
    }
) }}
